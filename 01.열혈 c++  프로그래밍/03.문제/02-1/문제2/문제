#문제 : 앞서 소개한 RefSwap.cpp의 SwapByRef2 함수를 다음의 형태로 호출하면 컴파일 에러가 발생한다.
  - SwapByRef2(23, 45);

컴파일 에러가 발생하는 이유가 무엇인지 설명한다.

------------------------

SwapByRef2함수의 매개변수로 int &ref1, int &ref2로 선언이 되었는데,
이는 23, 45와 같은 상수를 대상으로 참조를 할 수 없다. 
(만약, const int &ref1, const int &ref2로 매개변수가 선언되었다면 상수를 대상으로 참조가 가능하다. )
